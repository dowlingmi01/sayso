Description of the shell scripts in the bin directory
=====================================================

SUPPORTED COMMANDS:

1. Incremental database updates (set APPLICATION_ENV to your environment):

    cd [sayso_root]/bin
    export APPLICATION_ENV=development && ./sayso incremental

2. ALL updates with SVN update

    cd [sayso_root]/bin
    export APPLICATION_ENV=development && ./sayso-update-all
