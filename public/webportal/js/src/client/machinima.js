//Machinima extensions to our app.js
(function() {
    'use strict';

    var shared = {},

        client,
        version;

    client = 'recon.';
    version = '1.0';

    shared.client = client;
    shared.version = version;

    return shared;
})();