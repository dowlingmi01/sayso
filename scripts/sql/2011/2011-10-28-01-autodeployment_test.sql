SET foreign_key_checks = 0;

/**
 * This script does nothing,
 * just check an incremental log if its filename appears there
 * after having run the incremental update...
*/

SET foreign_key_checks = 1;
